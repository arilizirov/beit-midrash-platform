/** נשלח קישור — the post-submit screen (same for every input; no enumeration). */
export default function VerifyPage() {
  return (
    <main className="signin">
      <h1>בדקו את הדוא״ל</h1>
      <p>אם הכתובת מוזמנת לחבורה — נשלח אליה כעת קישור כניסה חד־פעמי.</p>
      <p>הקישור תקף לזמן מוגבל. אפשר לסגור חלון זה.</p>
    </main>
  );
}
